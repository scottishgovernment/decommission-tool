application:
  name: decommissioned

logging:
  level:
    org.springframework.web: WARN
    org.mygovscot: INFO

spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:decommissioned
    username: decommissioned
    password: decommissioned
  jpa:
    database: H2
    hibernate.ddl-auto: none
    generate-ddl: false
      
authentication:
    enabled: false
    endpoint: http://localhost:8086/security/
    
